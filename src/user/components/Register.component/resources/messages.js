import {defineMessages} from 'react-intl';

export default defineMessages({
    check_note: 'Please check the name is recognized correctly:',
    verify_note: 'Re-verification can be done by',
    link_title: 'clicking here',
    fix_note: 'If the name was filled in incorrectly, you can fix it in the fields below:'
});
