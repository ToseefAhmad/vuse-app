import {defineMessages} from 'react-intl';

export default defineMessages({
    is_exist_referral: 'You are already participating in the referral program',
    success_applied: 'The referral link was successfully applied'
});
